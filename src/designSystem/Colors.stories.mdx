import { Meta } from '@storybook/addon-docs/blocks';
import Color from '../component/ColorCard';
import colors from '../themes/colors';

<Meta title="Design System/Color" />

# Color

The Kdan official branding palette

<br />

## Primary palette

Primary colour are the base colors of the UI

<Preview withToolbar>
  <Color name="brand" hex={colors.brand} />
  <Color name="primary" hex={colors.primary} />
  <Color name="secondary" hex={colors.secondary} />
</Preview>

## Product palette

Kdan all products main colour.

<Preview withToolbar>
  <Color name="PDF" hex={colors.PDF} />
  <Color name="DS" hex={colors.DS} />
  <Color name="AD" hex={colors.AD} />
  <Color name="WO" hex={colors.WO} />
  <Color name="NL" hex={colors.NL} />
  <Color name="PM" hex={colors.PM} />
  <Color name="PS" hex={colors.PS} />
</Preview>

## Extended palette

### Neutrals

Neutral colours are used for non-interactive elements, backgrounds, borders and texts.

<Preview withToolbar>
  {Object.keys(colors)
    .filter((ele) => ele.match(/^N\d*\d$/g))
    .map((key) => (colors[key] ? <Color name={key} hex={colors[key]} /> : ''))}
</Preview>

### Others

<Preview withToolbar>
  {Object.keys(colors)
    .filter((ele) => ele.match(/^[^N]\d+/g))
    .map((key) => (colors[key] ? <Color name={key} hex={colors[key]} /> : ''))}
</Preview>

### Status

<Preview withToolbar>
  <Color name="error" hex={colors.error} />
  <Color name="warning" hex={colors.warning} />
  <Color name="info" hex={colors.info} />
  <Color name="success" hex={colors.success} />
</Preview>

## How to use?

```styled.js
import { colors } from "@kdanmobile/kdan-ui";

export const Wrapper = styled.div`
  color: ${colors.N0};
`;
```
